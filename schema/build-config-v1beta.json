{
  "$defs": {
    "BuildConfig": {
      "additionalProperties": false,
      "description": "BuildConfig is the writer input read from ocidoc.yaml/ocidoc.json or an explicitly selected configuration file.",
      "properties": {
        "annotations": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Annotations adds custom key/value pairs to the built artifact's root OCI manifest.\nKeys starting with \"org.ocidoc.\" are reserved for OCIDoc itself and rejected here.",
          "type": "object"
        },
        "components": {
          "additionalProperties": {
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "type": "array"
          },
          "description": "Components maps each component name to the path rules that select its files from the source tree.\nAt least one component with at least one rule is required.\nA name is either one of the standard component types\n(documentation, license, changelog, release-notes, security, contributing, code-of-conduct, support)\nor a custom \"x-\"-prefixed extension name.",
          "minProperties": 1,
          "propertyNames": {
            "pattern": "^(?:documentation|license|changelog|release-notes|security|contributing|code-of-conduct|support|x-[a-z][a-z0-9]*(?:-[a-z0-9]+)*)$"
          },
          "type": "object"
        },
        "document": {
          "$ref": "#/$defs/DocumentSettings",
          "description": "Document sets the built artifact's document identity (id, language, variant).\nOmit it to build the default, unlocalized document."
        },
        "entrypoints": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Entrypoints overrides the automatically detected primary file for one or more components, keyed by component name.\nA component not listed here falls back to automatic detection\n(for example, README.md for the documentation component).",
          "propertyNames": {
            "pattern": "^(?:documentation|license|changelog|release-notes|security|contributing|code-of-conduct|support|x-[a-z][a-z0-9]*(?:-[a-z0-9]+)*)$"
          },
          "type": "object"
        },
        "ignore": {
          "description": "Ignore lists path rules excluded from every component, applied after component matching.\nA rule prefixed with \"!\" is a negation: it restores a path an earlier ignore rule\n(here or in a component's own rules) would otherwise have excluded.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "schemaVersion": {
          "default": "v1beta",
          "description": "SchemaVersion is the build config format version.\nThe only currently supported value is \"v1beta\".",
          "enum": [
            "v1beta"
          ],
          "type": "string"
        },
        "settings": {
          "$ref": "#/$defs/BuildSettings",
          "description": "Settings controls build-time behavior that does not change the resulting artifact's logical identity:\nstrict mode and component compression.\nOmit it to use the documented defaults (strict mode off, gzip at level 6)."
        }
      },
      "required": [
        "schemaVersion",
        "components"
      ],
      "type": "object"
    },
    "BuildSettings": {
      "additionalProperties": false,
      "description": "BuildSettings holds writer-behavior options that are not part of the resulting artifact's identity.",
      "properties": {
        "compression": {
          "$ref": "#/$defs/CompressionSettings",
          "description": "Compression selects how OCIDoc component layers are compressed.\nOmit it to use the default: gzip at level 6."
        },
        "strict": {
          "default": false,
          "description": "Strict rejects a build where a declared component matches no files in the source tree,\ninstead of only warning about it.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "CompressionSettings": {
      "additionalProperties": false,
      "description": "CompressionSettings selects the component layer compression algorithm and, for gzip, its level.",
      "properties": {
        "level": {
          "default": 6,
          "description": "Level controls the compression level used by the selected compressor.\nHigher levels may reduce artifact size at the cost of additional CPU time during build.",
          "maximum": 9,
          "minimum": 0,
          "type": "integer"
        },
        "type": {
          "default": "gzip",
          "description": "Type selects the compression algorithm used for component layers.\nGzip is the default and has the broadest registry client support;\nzstd typically compresses faster and smaller at a comparable level,\nat the cost of less universal tooling support.",
          "enum": [
            "gzip",
            "zstd"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    },
    "DocumentSettings": {
      "additionalProperties": false,
      "description": "DocumentSettings sets the document identity tuple (see AnnotationDocumentID and related constants) for the artifact being built.",
      "properties": {
        "id": {
          "default": "default",
          "description": "ID identifies this artifact among multiple documentation artifacts\nthat may be attached to the same subject.",
          "type": "string"
        },
        "language": {
          "description": "Language identifies the document's language,\nwhen relevant to distinguishing it from other documents attached to the same subject.\nNo particular tag format is required or enforced;\nomit it for a language-neutral document.",
          "type": "string"
        },
        "variant": {
          "description": "Variant distinguishes multiple documents that share the same ID and language,\nfor example \"operator\" versus \"user\".\nOmit it when only one variant of this document exists.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "$id": "https://ocidoc.org/schema/build-config-v1beta.json",
  "$ref": "#/$defs/BuildConfig",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
